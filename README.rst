NaiveNeurals
============

This is naive implementation of perceptron neural network. Academic example only for educational purpose.
Still under development as backpropagation algorithm does not work properly,
treat as inspiration since it does not work... yet!